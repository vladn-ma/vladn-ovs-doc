02/19/2009
caould not build release for the fe2k-tools
after successfull build and all regression tests passed, following came out:

cd . && /bin/sh /home/vladn/views/release/fe2k-tools/config/missing --run autohe
Can't locate object method "path" via package "Autom4te::Request" at /usr/bin/au
autoheader: /usr/bin/autom4te failed with exit status: 1
make[2]: *** [config.h.in] Error 1
make[2]: Leaving directory `/projects/sbx-sdk_ext1/work/vladn/views/release/fe2k
make[1]: *** [check-recursive] Error 1
make[1]: Leaving directory `/projects/sbx-sdk_ext1/work/vladn/views/release/fe2k
make: *** [release_1.201] Error 2

12:39 PM
autoheader run with errors

one of suggested solutions found on the internet:
aclocal -I m4
libtoolize --copy --force
automake -a -c
autoconf -f
autoheader
./configure

12:57 PM
I removed the autom4te.cache and it compiled

1:01 PM
Updating 
libtool 
and 
autoconf
 to the lastest sid version seems to solve the problem. automake was already at the 
latest version.

1:20 PM
Package: autoconf

After upgrading autoconf to 2.58 (from 2.57), suddenly running
autoconf in a tree I was working on breaks like this:

asuffield@cyclone:~/arch/lookupd$ autoconf
Can't locate object method "path" via package "Request" at /usr/share/autoconf/Autom4te/C4che.pm line 69, <GEN1> line 112.

This appears to be caused by the internal data structures changing
(package Request has become Autom4te::Request), such that evaluating
autom4te.cache/request no longer yields a valid
@Autom4te::C4che::request array.

The best solution is probably for autom4te to store its own version
number in the cache, and only attempt to load it if the version
matches.

2:35 PM
autoreconf accepts the following options:

@option{--help}
@option{-h}
    Print a summary of the command line options and exit. 
@option{--version}
@option{-V}
    Print the version number of Autoconf and exit. 
@option{--verbose}
    Print the name of each directory where autoreconf runs autoconf (and autoheader, if appropriate). 
@option{--debug}
@option{-d}
    Don't remove the temporary files. 
@option{--force}
@option{-f}
    Remake even `configure' scripts and configuration headers that are newer than their input files (`configure.ac' and, if present, `aclocal.m4'). 
@option{--install}
@option{-i}
    Copy missing auxiliary files. This option is similar to the option --add-missing in automake. 
@option{--symlink}
@option{-s}
    Instead of copying missing auxiliary files, install symbolic links. 
@option{--localdir=dir}
@option{-l dir}
    Have autoconf and autoheader look for the package files `aclocal.m4' and (autoheader only) `acconfig.h' (but not `file.top' and `file.bot') in directory dir instead of in the directory containing each `configure.ac'. 
@option{--autoconf-dir=dir}
@option{-A dir}
    Override the location where the installed Autoconf data files are looked for. You can also 
    set the AC_MACRODIR environment variable to a directory; this option 
    overrides the environment variable. This option is rarely needed and dangerous; 
    it is only used when one plays with different versions of Autoconf simultaneously. 
@option{--m4dir=dir}
@option{-M dir}
    Specify location of additional macro files (`m4' by default). 

Additionally, the following options are recognized and passed to automake:

@option{--cygnus}
    Assume program is part of Cygnus-style tree. 
@option{--foreign}
    Set strictness to foreign. 
@option{--gnits}
    Set strictness to gnits. 
@option{--gnu}
    Set strictness to gnu. 
@option{--include-deps}
    Include generated dependencies in `Makefile.in'. 

0903161411
trying to use recomendations from phillip:

for bash
export AUTOCONFVER=2.63t
export LIBTOOLVER=1.5.8t
export PATH="~/myossbin:$PATH"

for tcsh:
setenv AUTOCONFVER 2.63t
setenv LIBTOOLVER 1.5.8t
setenv PATH "~/myossbin:$PATH"


cd $HOME
mkdir myossbin
cd myossbin
ln -s /tools/oss/etc/automake aclocal
ln -s /tools/oss/etc/automake aclocal-1.10 
ln -s /tools/oss/etc/autoconf autoconf 
ln -s /tools/oss/etc/autoconf autoheader 
ln -s /tools/oss/etc/autoconf autom4te 
ln -s /tools/oss/etc/automake automake 
ln -s /tools/oss/etc/automake automake-1.10 
ln -s /tools/oss/etc/autoconf autoreconf 
ln -s /tools/oss/etc/autoconf autoscan 
ln -s /tools/oss/etc/autoconf autoupdate 
ln -s /tools/oss/etc/autoconf ifnames 
ln -s /tools/oss/etc/libtool libtool 
ln -s /tools/oss/etc/libtool libtoolize

